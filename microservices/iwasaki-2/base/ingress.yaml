apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: iwasaki-2
  annotations:
    kubernetes.io/ingress.class: "gce"
    kubernetes.io/ingress.global-static-ip-name: "iwasaki-2"
spec:
  rules:
  - http:
      paths:
      - path: /*
        backend:
          serviceName: service
          servicePort: 8080
